/**Styles for SeatBookingHeader component */
import { makeStyles } from 'tss-react/mui';

export const useSeatBookingHeaderStyles = makeStyles()(() => ({
    headerDetails: {
        wordWrap: 'break-word',
        overflowWrap: 'break-word',
    },
}));
